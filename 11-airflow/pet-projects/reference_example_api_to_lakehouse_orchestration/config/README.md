# Shared Config Reference

The reusable config template behind this runnable example lives in:

- `shared/configs/templates/airflow/api_runtime.example.json`

Keep `api_runtime.json` here only as the local runnable copy used by the reference DAG.