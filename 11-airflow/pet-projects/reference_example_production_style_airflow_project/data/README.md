# Shared Expected Output Reference

The reusable output contract behind this runnable example lives in:

- `shared/testing/expected-outputs/airflow/orders_platform_contract.json`

Keep `expected_output_contract.json` here only as the local runnable copy used by the reference example and tests.