# Source Code

Implement query and aggregation examples here.

Suggested shape:

- `queries/` for operational reads
- `aggregations/` for event analysis pipelines
- `helpers/` for small load or validation scripts if needed
