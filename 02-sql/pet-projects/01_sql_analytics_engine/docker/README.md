# Docker

Docker is optional here.

Use this directory only if you want a small local SQL runtime such as:

- Postgres with seed data
- a shell script for loading sample tables
- run notes for validating the queries end to end
