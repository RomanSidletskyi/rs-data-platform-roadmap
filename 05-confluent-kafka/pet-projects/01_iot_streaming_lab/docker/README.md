# Docker Assets

Suggested local stack:

- Kafka broker
- optional Schema Registry
- optional UI or CLI helpers

Keep the stack minimal enough for learning, but realistic enough to inspect topics and replay.