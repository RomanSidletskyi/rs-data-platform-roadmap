# Source Assets

Starter assets in this folder:

- `build_order_journey_summary.py` builds a downstream order journey projection from mixed domain events
- `check_event_backbone_contracts.py` performs simple contract and ownership checks against sample events

Keep ownership boundaries and correlation IDs explicit.