# Data Assets

Provided sample files:

- `sample_order_events.jsonl` with valid order events, including a duplicate event for idempotency discussion
- `sample_bad_order_events.jsonl` with malformed records for DLQ practice
- `sample_target_table_schema.sql` with a target-table shape for the sink lab