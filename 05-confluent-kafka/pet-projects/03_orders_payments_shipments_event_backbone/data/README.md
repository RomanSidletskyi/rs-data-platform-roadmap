# Data Assets

Sample data included in this project:

- `sample_backbone_events.jsonl` contains valid order, payment, and shipment events for two orders
- `sample_bad_backbone_events.jsonl` contains malformed or weakly-modeled events for contract review practice

Use these files to practice:

- domain ownership reasoning
- downstream correlation by `order_id`
- contract review and anti-pattern detection