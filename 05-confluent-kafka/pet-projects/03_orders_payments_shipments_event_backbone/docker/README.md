# Docker Assets

Suggested local stack:

- Kafka broker
- optional Schema Registry
- optional lightweight UI or CLI tools for topic inspection

Focus on contract inspection and multi-topic event flow rather than cluster complexity.