# Test Assets

Provided fixture:

- `fixture_expected_session_summary.json` for validating the output of `build_session_summary.py`
- `verify_session_summary.sh` for a quick fixture-driven smoke check

Suggested checks:

- event contract validation
- key-selection assumptions
- session-summary output shape
- replayability of raw clickstream input