# Shared Script References

Reusable helper scripts for this reference example now live in:

- `shared/scripts/helpers/dbt/render_profiles.py`
- `shared/scripts/helpers/dbt/summarize_run_results.py`

Keep only project-specific helpers inside the module itself.