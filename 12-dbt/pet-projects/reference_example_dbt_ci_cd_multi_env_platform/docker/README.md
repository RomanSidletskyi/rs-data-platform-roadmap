# Docker

This reference example does not require a full container stack.

Use Docker only if you want:

- a local runner image close to CI
- a repeatable docs-serving environment
- an easy wrapper around environment variables and dbt commands
