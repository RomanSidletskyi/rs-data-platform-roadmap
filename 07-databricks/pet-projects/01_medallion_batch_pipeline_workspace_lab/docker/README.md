# Docker Assets

This project is Databricks-first rather than local-runtime-first.

If you add local helpers, use Docker only for:

- fixture generation
- lightweight validation helpers
- local comparison workflows