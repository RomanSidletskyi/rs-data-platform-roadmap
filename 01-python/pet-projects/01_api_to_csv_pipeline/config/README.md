# Config

Use this directory for runtime config examples such as:

- API URL and timeout settings
- selected fields to keep
- raw and processed output locations

`config.yaml` is a starter asset, not the finished project by itself.