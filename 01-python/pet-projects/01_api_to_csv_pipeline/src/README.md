# Source Code

Implement the project here.

Suggested shape:

- `main.py` as entrypoint
- `api_client.py` for HTTP access
- `processor.py` for field selection and transformation
- `writer.py` for raw and processed output persistence