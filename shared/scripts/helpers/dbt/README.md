# dbt Helper Scripts

This directory contains small reusable helper scripts for local dbt workflows and CI/CD support.

Good examples:

- profile rendering hints
- artifact summary helpers
- small operational wrappers that are not tied to one project only

Rule:

- keep helpers lightweight and reviewable
- avoid project-specific business logic here