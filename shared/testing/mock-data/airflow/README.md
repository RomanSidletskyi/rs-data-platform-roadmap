# Airflow Mock Data

This directory contains reusable small mock payloads for Airflow workflow examples.

Use it for:

- API response samples
- raw payload fixtures
- tiny contract-driven test inputs

Rule:

- keep data synthetic and small
- avoid scenario-specific noise when a more generic payload is enough