# Data

This folder contains small replayable inputs that approximate what a raw landed batch window might look like.

Use them to reason about:

- schema quality
- malformed rows
- cleanup rules
- daily aggregation grain