# Solution

Useful commands:

    git remote -v
    git fetch origin
    git branch -vv

Reasoning:

- `fetch` downloads remote state without changing the current branch
- `pull` usually fetches and then integrates changes into the current branch