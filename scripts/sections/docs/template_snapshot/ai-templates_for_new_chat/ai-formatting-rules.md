# AI Markdown Formatting Rules

When generating Markdown files in chat responses:

Always format code blocks using **4 space indentation**.

Do NOT use triple backticks.

Example:

    module/
        README.md
        learning-materials/
        simple-tasks/
        pet-projects/

This prevents formatting issues when copying Markdown into repository files.