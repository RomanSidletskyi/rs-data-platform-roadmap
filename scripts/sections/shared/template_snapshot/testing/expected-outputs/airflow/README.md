# Airflow Expected Outputs

This directory contains reusable expected-output contracts for Airflow workflow examples.

Use it for:

- publish directory contracts
- success marker conventions
- minimum output expectations

Rule:

- keep contracts simple and reusable
- keep business-specific semantics inside the module when needed