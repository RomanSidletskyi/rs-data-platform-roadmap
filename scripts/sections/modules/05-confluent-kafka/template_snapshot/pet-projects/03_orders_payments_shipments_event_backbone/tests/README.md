# Test Assets

Included checks:

- fixture comparison for downstream order journey summary
- contract review output for malformed events and ownership violations

Run `tests/verify_backbone_assets.sh` after editing the starter scripts.