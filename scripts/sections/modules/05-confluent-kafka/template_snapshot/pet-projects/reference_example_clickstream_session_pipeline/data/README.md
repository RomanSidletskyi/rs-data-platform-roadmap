# Data Assets

Provided sample files:

- `sample_clickstream_events.jsonl` with page views, product clicks, cart actions, and checkout events across multiple sessions

The records use explicit fields such as `event_id`, `event_type`, `event_time`, `session_id`, `user_id`, and `page_url`.