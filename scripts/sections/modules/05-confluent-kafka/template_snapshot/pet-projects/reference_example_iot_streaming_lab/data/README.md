# Data Assets

Provided sample files:

- `sample_telemetry_events.jsonl` with valid telemetry events across two warehouse sites
- `sample_bad_telemetry_events.jsonl` with malformed records for validation and DLQ practice

The records use realistic fields such as `device_id`, `site_id`, `temperature_c`, `humidity_pct`, and `event_time`.