# Starter Notes For src

Implement the processing application here.

Recommended minimum responsibilities:

- read the input dataset from `INPUT_PATH`
- connect to Postgres and MinIO using runtime settings
- write at least one output to one platform service
- optionally write to both to show the difference between structured and object storage