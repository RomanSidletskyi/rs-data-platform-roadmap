# Starter Notes For docker

Create Docker assets for the guided project here.

Recommended minimum:

- one `Dockerfile`

The Docker image should:

- use a Python base image
- copy only required source files
- run the batch job through a clear default command
- rely on mounted `data/` and `config/` at runtime