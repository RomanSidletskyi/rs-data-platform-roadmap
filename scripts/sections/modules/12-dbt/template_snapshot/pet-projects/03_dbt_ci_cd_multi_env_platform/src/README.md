# Source Code

This directory may contain helper scripts such as:

- schema-routing macro based on `shared/templates/dbt/macros/generate_schema_name.sql`
- lightweight wrappers inspired by `shared/scripts/helpers/dbt/`

- local bootstrap helpers
- artifact parsing scripts
- environment variable loaders
- local docs generation wrappers

Keep the operational logic here lightweight and reviewable.
