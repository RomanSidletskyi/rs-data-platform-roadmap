# Docker

Optional local execution support:

- dbt execution image
- local docs serving
- local environment bootstrap runner

Use Docker here only if it makes CI parity or local docs generation easier.
