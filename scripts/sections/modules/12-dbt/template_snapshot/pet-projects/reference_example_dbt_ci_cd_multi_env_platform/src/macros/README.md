# Shared Macro Reference

The reusable `generate_schema_name.sql` macro for this project now lives in:

- `shared/templates/dbt/macros/generate_schema_name.sql`

Use the shared version as the source of truth for environment-safe schema naming patterns.