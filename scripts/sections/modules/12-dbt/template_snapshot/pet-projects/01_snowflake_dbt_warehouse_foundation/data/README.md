# Data

Use this directory for small learning assets and contract notes, not for large warehouse dumps.

Good contents for this project:

- notes describing the raw orders, customers, and products tables
- tiny sample payloads that explain key edge cases
- an expected-output note for marts such as `dim_customer` and `fct_orders`

Large warehouse extracts should stay outside the repo.
