# Shared Config Reference

The reusable config template behind this runnable example lives in:

- `shared/configs/templates/airflow/runtime_config.example.json`

Keep `runtime_config.json` here only as the local runnable copy used by the reference DAG.