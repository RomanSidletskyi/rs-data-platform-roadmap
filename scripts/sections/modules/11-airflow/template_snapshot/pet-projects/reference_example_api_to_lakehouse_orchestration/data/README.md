# Shared Mock Data Reference

The reusable payload sample behind this runnable example lives in:

- `shared/testing/mock-data/airflow/sample_api_response.json`

Keep `sample_api_response.json` here only as the local runnable copy used by the reference example and tests.