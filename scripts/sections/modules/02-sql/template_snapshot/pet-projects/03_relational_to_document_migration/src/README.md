# Source Code

Use this directory for migration artifacts.

Suggested shape:

- `sql/` for source-model queries
- `mongo/` for target-model query examples
- `notes/` for side-by-side comparison writeups
