# Docker

Docker is optional here.

Use this directory only if you want a small local playground such as:

- Postgres plus MongoDB containers
- import notes for source and target examples
- simple scripts for comparing query paths locally
