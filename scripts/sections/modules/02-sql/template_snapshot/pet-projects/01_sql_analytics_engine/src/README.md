# Source Code

Implement the analytical SQL here.

Suggested shape:

- `source_model.sql` or notes for grain understanding
- `kpi/` for reusable metric outputs
- `customer/` for customer analytics
- `product/` for product analytics
- `behavior/` for funnel and retention logic
