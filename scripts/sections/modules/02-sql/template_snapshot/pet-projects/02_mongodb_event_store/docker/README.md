# Docker

Docker is optional here.

Use this directory only if you want a tiny local MongoDB runtime such as:

- a MongoDB container with seed events
- import notes for sample payloads
- run instructions for testing queries locally
