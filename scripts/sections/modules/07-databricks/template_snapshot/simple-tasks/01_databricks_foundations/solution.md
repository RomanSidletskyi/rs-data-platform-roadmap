# Solution

Databricks explanation:

- Databricks is a managed lakehouse platform that provides compute, workspace collaboration, jobs, governance, and SQL delivery around data workloads.

Boundary explanation:

- Spark is the compute engine
- Databricks is the managed platform around that engine
- Databricks does not replace storage or BI semantics by itself

Notebook promotion example:

- parameterize inputs
- version the code in source control
- move execution into a repeatable job definition