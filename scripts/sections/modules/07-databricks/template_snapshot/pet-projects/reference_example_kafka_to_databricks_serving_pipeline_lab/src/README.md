# Source Assets

- `build_order_serving_projection.py` previews the gold serving projection from landed events
- `check_serving_contract.py` validates the Databricks job and warehouse-facing contract files