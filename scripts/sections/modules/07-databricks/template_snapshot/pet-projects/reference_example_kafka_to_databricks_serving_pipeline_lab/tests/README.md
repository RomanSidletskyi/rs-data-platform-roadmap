# Test Assets

- `fixture_expected_serving_projection.json` validates the gold serving projection shape
- `fixture_expected_serving_check.txt` validates the contract checker
- `verify_serving_assets.sh` runs both checks