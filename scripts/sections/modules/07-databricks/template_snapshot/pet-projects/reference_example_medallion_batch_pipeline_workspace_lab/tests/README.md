# Test Assets

- `fixture_expected_daily_revenue.json` validates the local gold preview output
- `fixture_expected_job_check.txt` validates the job-definition review helper
- `verify_medallion_assets.sh` runs both checks