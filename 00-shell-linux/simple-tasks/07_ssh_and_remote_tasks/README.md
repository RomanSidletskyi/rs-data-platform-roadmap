# SSH And Remote Tasks

## Task 1 - Write An SSH Config Entry

### Goal

Normalize remote access through aliases.

### Requirements

- write a sample `Host` entry with hostname, user, and identity file

## Task 2 - Compare scp And rsync

### Goal

Know when each transfer tool fits better.

### Requirements

- explain one case for `scp`
- explain one case for `rsync`

## Task 3 - Debug A Failed SSH Session

### Goal

List the command sequence you would use if `ssh host` fails.