# CI Notes

This example uses:

- `compileall` for a lightweight structural Python validation step
- `unittest` for deterministic built-in test execution
- a version matrix to show how one workflow definition expands into several runs